---
import Layout from "@/layouts/Layout.astro";
import LanguageToggle from "@/components/LanguageToggle.astro";
import ThemeToggle from "@/components/ThemeToggle.astro";

import Differential from "@/components/sections/Differential.astro";
import Experience from "@/components/sections/Experience.astro";
import Hero from "@/components/sections/Hero.astro";
import Projects from "@/components/sections/Projects.astro";
import Skills from "@/components/sections/Skills.astro";
import Contact from "@/components/sections/Contact.astro";

import { getCV } from "@/i18n/utils";

const currentLang = "en";
const cvData = await getCV(currentLang);
const { name } = cvData.basics;
---

<Layout
  title={`${name} | Analysis, implementation, and product`}
  description="Ramiro Nicolás Cosa's portfolio: functional analysis, implementation, and digital product development, with experience in healthcare and operations."
  lang={currentLang}
  alternateUrl="/"
  isHome
>
  <main>
    <Hero lang={currentLang} />
    <Differential lang={currentLang} />
    <Projects lang={currentLang} />
    <Experience lang={currentLang} />
    <Skills lang={currentLang} />
    <Contact lang={currentLang} />
  </main>

  <LanguageToggle />
  <ThemeToggle />
</Layout>

<style>
  main {
    padding: 4rem;
    margin: auto;
    width: 100%;
  }

  @media (width <= 700px) {
    main {
      padding: 2rem 1.25rem;
    }
  }
</style>
